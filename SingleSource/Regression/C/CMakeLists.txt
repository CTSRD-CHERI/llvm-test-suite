list(APPEND LDFLAGS -lm)
set(ignored)
if (CHERI)
    # XXXAR: https://github.com/CTSRD-CHERI/clang/issues/163 (computed goto)
    list(APPEND ignored 2004-03-15-IndirectGoto)
endif()
llvm_singlesource(PREFIX "Regression-C-" IGNORED_TESTS "${ignored}")
